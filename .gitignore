.DS_Store
.env
.env.*
node_modules/
dist/
build/
.cache/
*.log
.publish

# Local-only dev tooling for the App Store Insights project: raw review
# snapshots, Run A/B analysis drafts, and scripts. Never publish this —
# only the curated output in projects/app-store-insights/data/ is public.
_tools/

# Full-resolution Kynd screenshots and print PDFs used to cut the site
# assets. Source material only — the optimized crops the site actually
# serves live in assets/img/kynd/ and are tracked.
Kynd project/
